import React from "react";
import {Layout} from "antd";
import styles from "./FooterContainer.less"

const Footer = Layout.Footer;

export default () => {
  return (
    <Footer className={styles.footer}></Footer>
  )
}
